# `services` folder

This folder contains **backend services** for the monorepo: APIs, workers, automation backends, and operational services that are not user interfaces.

- **Main purpose**: keep server-side applications separate from UI applications while preserving a shared monorepo workflow.
- **Recommendation**: each service should document its runtime, environment variables, data dependencies, and local run commands.
