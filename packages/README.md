# `packages` folder

This folder contains reusable code packages shared across services, interfaces, agents, or workflows.

- `shared/`: canonical shared package location for repo-level types, contracts, and utilities.
- Recommendation: document each package public API and the consumers that depend on it.
